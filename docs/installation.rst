============
Installation
============

At the command line::

    $ easy_install django-mantis-actionables

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-mantis-actionables
    $ pip install django-mantis-actionables
